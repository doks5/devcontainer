The `assets` folder is meant to store the ssh public file
and a file with the `gpg` key for the user whose git configuration
is being set up.

If there are multiple ssh or gpg keys to be configured, each needs
to be in a separate file.
